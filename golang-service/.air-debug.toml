root = "."
tmp_dir = "tmp"

[build]
  args_bin = []
  bin = "./tmp/main"
  cmd = "go build -gcflags='all=-N -l' -o ./tmp/main ."
  full_bin = "go run github.com/go-delve/delve/cmd/dlv exec --continue --accept-multiclient --log --headless --listen :4040 --api-version 2 ./tmp/main"
